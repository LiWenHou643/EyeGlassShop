import styled from 'styled-components';

const Section = styled.section`
    &.p-section {
        margin-top: 40px;
        margin-bottom: 40px;
        padding: 0 8px;
    }
`;

export default Section;
